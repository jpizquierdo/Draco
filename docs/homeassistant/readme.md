# Home Assistant Integration

Add all `.yaml` files in this directory to your `configuration.yaml` **except** `lovelace_watering.yaml`.

`lovelace_watering.yaml` contains the YAML for a custom Lovelace card — paste its contents into a **Manual card** in the Home Assistant UI.
